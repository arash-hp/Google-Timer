import { Grid, Typography } from "@mui/material"
import Content from "./Content";

const Project = ()=>{
    return <Grid container sx={{justifyContent:'center',alignItems:'center'}}>
        <Content />
    </Grid>
}
 export default Project;